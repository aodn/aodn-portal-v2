import icon_img from "@/assets/logos/csiro-logo.png";
import IconContainer from "../IconContainer";

const CSIROIcon = () => {
  return <IconContainer imgUrl={icon_img} sx={{ border: "none" }} />;
};

export default CSIROIcon;
