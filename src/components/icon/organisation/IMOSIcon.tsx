import IconContainer from "../IconContainer";
import imos from "@/assets/logos/imos-logo-transparent.png";

const IMOSIcon = () => {
  return <IconContainer imgUrl={imos} sx={{ border: "none" }} />;
};

export default IMOSIcon;
