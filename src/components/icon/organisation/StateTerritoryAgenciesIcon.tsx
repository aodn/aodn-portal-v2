import IconContainer from "../IconContainer";
import { StateTerritoryAgenciesSVGIcon } from "../../../assets/filter-organisations/state_territory_agencies";

const StateTerritoryAgenciesIcon = () => {
  return (
    <IconContainer
      icon={StateTerritoryAgenciesSVGIcon}
      sx={{ border: "none" }}
    />
  );
};

export default StateTerritoryAgenciesIcon;
