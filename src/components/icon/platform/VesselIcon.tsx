import IconContainer from "../IconContainer";
import { VesselSVGIcon } from "../../../assets/filter-platforms/vessel";

const VesselIcon = () => {
  return <IconContainer icon={VesselSVGIcon} sx={{ border: "none" }} />;
};

export default VesselIcon;
