import IconContainer from "../IconContainer";
import { GliderSVGIcon } from "../../../assets/filter-platforms/glider";

const GliderIcon = () => {
  return <IconContainer icon={GliderSVGIcon} sx={{ border: "none" }} />;
};

export default GliderIcon;
