import IconContainer from "../IconContainer";
import { AircraftSVGIcon } from "../../../assets/filter-platforms/aircraft";

const AircraftIcon = () => {
  return <IconContainer icon={AircraftSVGIcon} sx={{ border: "none" }} />;
};

export default AircraftIcon;
