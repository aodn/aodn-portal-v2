import IconContainer from "../IconContainer";
import { BioPlatformSVGIcon } from "../../../assets/filter-platforms/bio_platform";

const BioPlatformIcon = () => {
  return <IconContainer icon={BioPlatformSVGIcon} sx={{ border: "none" }} />;
};

export default BioPlatformIcon;
