import { FloatSVGIcon } from "../../../assets/filter-platforms/float";
import IconContainer from "../IconContainer";

const FloatIcon = () => {
  return <IconContainer icon={FloatSVGIcon} sx={{ border: "none" }} />;
};

export default FloatIcon;
