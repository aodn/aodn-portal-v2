import IconContainer from "../IconContainer";
import { RadarSVGIcon } from "../../../assets/filter-platforms/radar";

const RadarIcon = () => {
  return <IconContainer icon={RadarSVGIcon} sx={{ border: "none" }} />;
};

export default RadarIcon;
