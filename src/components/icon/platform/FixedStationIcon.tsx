import IconContainer from "../IconContainer";
import { FixedStationSVGIcon } from "../../../assets/filter-platforms/fixed_station";

const FixedStationIcon = () => {
  return <IconContainer icon={FixedStationSVGIcon} sx={{ border: "none" }} />;
};

export default FixedStationIcon;
