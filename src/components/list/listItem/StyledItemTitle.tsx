import { Grid } from "@mui/material";
import { styled } from "@mui/material/styles";

const StyledItemTitle = styled(Grid)(() => ({
  display: "flex",
  justifyContent: "flex-start",
  alignItems: "center",
}));

export default StyledItemTitle;
