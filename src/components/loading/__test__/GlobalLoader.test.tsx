import { render } from "@testing-library/react";
import GlobalLoader from "../GlobalLoader";

describe.skip("globalLoader", () => {
  beforeEach(() => {
    render(<GlobalLoader />);
  });
  // TODO: deal with the test later
});
