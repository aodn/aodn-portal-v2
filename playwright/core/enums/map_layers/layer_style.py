from enum import Enum, auto


class LayerStyle(Enum):
    CLUSTERED = auto()
    UNCLUSTERED = auto()
    HEAT_MAP = auto()
    SPIDER = auto()
    GEO_SERVER = auto()
    HEX_GRID = auto()
    SPATIAL_EXTENT = auto()
